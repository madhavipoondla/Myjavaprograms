// Java Program to Demonstrate Method Overloading and Object Method Calls

// Define a class named Test
public class Test {

    // Method to add two integers
    public int addition(int a, int b) {
        return a + b;
    }

    // Overloaded method to add two double values
    public double addition(double a, double b) {
        return a + b;
    }

    // Method to multiply two integers
    public int mul(int a, int b) {
        return a * b;
    }

    // Main method - entry point of the program
    public static void main(String[] args) {
        // Creating an object of the Test class
        Test test1 = new Test();

        // Calling addition method with integer parameters
        int result = test1.addition(25, 25);
        System.out.println("Addition of integers: " + result);

        // Calling overloaded addition method with double parameters
        double result1 = test1.addition(3.0, 2.4);
        System.out.println("Addition of doubles: " + result1);

        // Calling multiplication method
        int result2 = test1.mul(2, 3);
        System.out.println("Multiplication: " + result2);
    }
}
